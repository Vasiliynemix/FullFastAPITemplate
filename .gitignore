# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# ============================================================
# Env / secrets — .env НЕ коммитим, .env.example коммитим
# ============================================================
.env
.env.*
!.env.example
*.local

# ============================================================
# Инструменты / тесты / покрытие
# ============================================================
.ruff_cache/
.pytest_cache/
.mypy_cache/
.benchmarks/
.coverage
coverage.xml
htmlcov/

# uv lock-файл (для приложения обычно коммитят ради воспроизводимости;
# в этом шаблоне образ ставит зависимости из pyproject, поэтому lock игнорируем)
uv.lock

# ============================================================
# Docker / certbot (локальные тома, не в git)
# ============================================================
docker/certbot/conf/
docker/certbot/www/

# ============================================================
# Логи приложения (bind-mount ./logs)
# ============================================================
logs/

# ============================================================
# IDE / редакторы / ОС / инструментарий ассистентов
# ============================================================
.idea/
.vscode/
.DS_Store
*.swp
.claude/
